// Get the sprite from another object
sprite_index = object_get_sprite(gun);

// Set a random angle for the sprite
image_angle = irandom_range(0, 359); // Random angle between 0 and 359 degrees

image_xscale = scale
image_yscale = scale