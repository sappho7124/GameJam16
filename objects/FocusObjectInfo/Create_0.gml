// Define the infobox properties
infobox_x_offset = 100;        // Offset for X (center-based)
infobox_y_offset = -150;        // Offset for Y (center-based)
infobox_box_width = 100;      // Default width of the infobox
infobox_box_height = 200;     // Default height of the infobox
infobox_margin = 10;          // Margin for text inside the box